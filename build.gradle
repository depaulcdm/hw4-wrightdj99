group 'edu.depaul.cdm.se441'
version '1.0-SNAPSHOT'

apply plugin: 'java'

repositories{
	mavenCentral()
}

dependencies{
	implementation "log4j:log4j:1.2.17"
	testImplementation "junit:log4j:4.12"
}

jar{
	baseName = project.name + '-all'
	from{
		configurations.runtimeClasspath.findAll { it.name.endsWith('jar') 
			}.collect { zipTree(it) }
	}
	manifest{
		attributes('Main-Class': 'hw4-wrightdj99.hello-world.src.main.java.hello.HelloWorld')
	}
}
